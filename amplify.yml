nversion: 0.1
frontend:
  phases:
    build:
      commands:
        - npm run build
  
  cache:
    paths:
      - node_modules/**/*
test:
  artifacts:
    baseDirectory: cypress
    configFilePath: "**/mochawesome.json"
    files:
      - "**/*.png"
      - "**/*.mp4"
  phases:    
    test:
      commands:
        - npm test --record